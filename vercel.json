{
  "buildCommand": "pnpm build",
  "outputDirectory": ".output",
  "installCommand": "pnpm install",
  "framework": null,
  "devCommand": "pnpm dev",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ],
  "env": {
    "NODE_VERSION": "22"
  }
}
